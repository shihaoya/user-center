cd /d/stady/yp/user-center

# ① 初始化（如果还没提交过）
git init
git commit --allow-empty -m "init"

# ② 添加远程并 fetch
git remote add repo_client ./_client
git remote add repo_server ./_server
git fetch repo_client
git fetch repo_server

# ③ subtree add
git subtree add --prefix=client repo_client master
git subtree add --prefix=server repo_server master

# ④ 清理
git remote remove repo_client
git remote remove repo_server
rm -rf _client _server